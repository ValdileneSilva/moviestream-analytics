name: 'moviestream_analytics'
version: '1.0'
config-version: 2

profile: 'moviestream_analytics'

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_modules"]

models:
  moviestream_analytics:
    marts:
      +materialized: table
    staging:
      +materialized: view
